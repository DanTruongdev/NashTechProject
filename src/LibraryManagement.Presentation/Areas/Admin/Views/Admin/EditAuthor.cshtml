@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model LibraryManagement.Core.Entities.Author;
@{

    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Edit Author";
}
<div class="row justify-content-center mt-5">
    <div class="col-lg-6">
        <div class="card">
            <div class="card-header text-center">
                <h3>Edit author</h3>
            </div>
            <form class="card-body card-block" method="post" asp-area="Admin" asp-controller="Admin" asp-action="EditAuthor">
                <div class="has-success form-group">
                    <input asp-for="AuthorId" type="text" value="@Model.AuthorId" hidden>
                    <label for="inputSuccess2i" class=" form-control-label"><strong>Author Name:</strong></label>
                    <input asp-for="AuthorName" type="text" value="@Model.AuthorName" id="inputSuccess2i" placeholder="enter here..." class="form-control-success form-control">
                    <span class="text-danger" asp-validation-for="AuthorName"></span>
                    <span class="text-danger">@ViewBag.EditAuthMsg</span>
                </div>

                <div class="text-center">
                    <!-- Center the button -->
                    <button type="submit" class="btn btn-primary btn-sm">
                        <i class="fa fa-dot-circle-o"></i> Submit
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>